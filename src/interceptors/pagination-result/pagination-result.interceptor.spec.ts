import { PaginationResultInterceptor } from './pagination-result.interceptor';

describe('PaginationResultInterceptor', () => {
  it('should be defined', () => {
    expect(new PaginationResultInterceptor()).toBeDefined();
  });
});
