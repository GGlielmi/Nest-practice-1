import { EntityNotFoundInterceptor } from './entity-not-found.interceptor';

describe('EntityNotFoundInterceptor', () => {
  it('should be defined', () => {
    expect(new EntityNotFoundInterceptor()).toBeDefined();
  });
});
